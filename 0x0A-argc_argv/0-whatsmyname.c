#include <stdio.h>
/**
 * main - program to print its name
 * @argc - argument count
 * @argv - array of pointers to parameters
 *
 * Retrun: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
